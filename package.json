{
  "name": "landfello",
  "version": "1.0.0",
  "description": "Land marketplace for buying and selling land in Africa",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all": "npm install --prefix frontend && python -m pip install -r backend/requirements.txt",
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --app-dir backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test:backend": "python -m pytest backend/tests -q",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
